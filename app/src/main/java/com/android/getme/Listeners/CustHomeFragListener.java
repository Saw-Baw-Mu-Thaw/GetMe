package com.android.getme.Listeners;

public interface CustHomeFragListener {
    // define callback methods for when customer clicks search bar, standard ride
    // economy or bike
    public void onBookRideClicked(String vehicleType);
}
